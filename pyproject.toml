[tool.poetry]
name = "jaundice-assistant"
version = "0.1.0"
description = "Web application to assist in managing jaundice cases."
authors = ["Nawat Ngerncham <n.ngerncham@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "src"}]

[tool.poetry.dependencies]
python = "^3.10"
flask = "^2.3.2"
fhir-resources = "^7.0.1"
numpy = "^1.24.2"
pandas = "^2.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"


[tool.poetry.scripts]
dev = "src.main:dev"